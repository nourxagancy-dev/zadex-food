# منطق إرسال الطلب على واتساب

بعد إنشاء الطلب في Laravel يتم:
1. توليد `order_number` مثل `RST-YYMMDD-####` باستخدام Prefix خاص بالمطعم.
2. جمع بيانات الطلب (العميل، نوع الطلب، العناصر، الإضافات، الأسعار، طريقة الدفع، حالة الدفع، الملاحظات).
3. إنشاء رسالة نصية منسقة بالعربية مع أسطر مفصولة بـ `%0A` (ترميز URL).
4. تخزين النص في حقل `orders.whatsapp_message` مع رابط جاهز `https://wa.me/<number>?text=<encoded>` حيث الرقم من إعدادات المطعم/الفرع.
5. إرجاع رابط واتساب في استجابة API وتخزينه لزر «فتح واتساب» في الواجهة.

## قالب الرسالة المقترح
```
🚀 طلب جديد رقم {{order_number}}
الاسم: {{customer_name}} | الجوال: {{customer_phone}}
النوع: {{order_type}} {{delivery_or_branch}}
---
{{#items}}
• {{name}} x{{qty}} — {{total}} {{currency}}
  {{#options}}- {{name}} (+{{price}}){{/options}}
{{/items}}
---
المجموع: {{subtotal}} {{currency}}
الخصم: {{discount}} {{currency}}
الضريبة: {{tax}} {{currency}}
التوصيل: {{delivery_fee}} {{currency}}
الإجمالي: {{total}} {{currency}}
الدفع: {{payment_method}} — حالة: {{payment_status}}
ملاحظات: {{notes}}
```

## سيناريوهات الدفع
- **Paymob**: بعد webhook ناجح يتم تحديث `payment_status=paid` وتعديل الرسالة لتشمل حالة الدفع النهائية.
- **فوري/فودافون كاش**: عند استقبال إثبات يدوي تُحدّث الحالة إلى `waiting_payment` أو `paid` ويعاد توليد النص.
- **عند الاستلام**: الحالة تبقى `unpaid` حتى اكتمال التسليم.
